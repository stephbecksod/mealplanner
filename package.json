{
  "name": "meal-planner",
  "version": "1.0.0",
  "description": "A browser-based dinner planning app with smart grocery lists and beverage pairings",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "keywords": ["meal-planning", "recipes", "grocery-list"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
